import React from 'react'
import InfoCard from '../components/InfoCard'
import { Container } from '@mui/material'

const LinksPageBottom = ({ infoPoints})=>{
	// return(
	// 	<>
	// 		<Container>
	// 			{infoPoints.map(point => {
	// 				return (
	// 					<InfoCard key={point.id} point={point}/>
	// 				)
	// 			})}
	// 		</Container>
	// 	</>
	// )
}

export default LinksPageBottom