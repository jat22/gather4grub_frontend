import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders learn react link', () => {
//   render(<App />);
//   const linkElement = screen.getAllByText(/Gather4Grub/i);
//   expect(linkElement.length).toEqual(3);
// });

console.log("test")